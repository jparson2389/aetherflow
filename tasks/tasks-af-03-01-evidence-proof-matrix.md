## Relevant Files

- `docs/evidence/AF-03-01.md` - Evidence pack; proof matrix **AC2** row must align **Evidence**, **Entry Point**, and **Failure Coverage** with cited tests (audit blocking item).
- `logs/verification/AF-03-01.json` - Regenerated by canonical regrade after evidence edits.
- `docs/requirements-report.md` - Regenerated alongside verification JSON.
- `tests/integration/test_capture_stability.py` - Source of truth for AC2 metrics/sustained-drop behavior cited in matrix (`CaptureMetrics` / `CaptureMetricsTracker`).
- `tests/integration/test_capture_opencv.py` - Optional cite for OpenCV-path measurability (`test_opencv_capture_start_stop_yields_measurable_metrics`).
- `tests/ui/test_capture_mode_matrix.py` - Optional strengthener for AC1 if “selectable” includes the capture panel UI.
- `src/aetherflow/core/capture_metrics.py` - `is_sustained_drop` semantics (90% FPS band, duration, drop-rate branch) for accurate **Threshold** prose.

### Notes

- Source audit: `logs/audit/evidence-proof-matrix-AF-03-01-20260412-080819.md`.
- Sign-off is **not** safe until the AC2 matrix defect is fixed; optional items do not block accuracy of the matrix.
- After evidence edits, run `uv run python -m tools.verify_requirements` so `logs/verification/AF-03-01.json` and `docs/requirements-report.md` stay consistent.

## Instructions for Completing Tasks

**IMPORTANT:** As you complete each task, check it off by changing `- [ ]` to `- [x]`.

## Tasks

- [ ] 0.0 Create feature branch
  - [ ] 0.1 Create and checkout a branch for this work (e.g. `git checkout -b fix/af-03-01-evidence-matrix`)

- [x] 1.0 Fix AC2 proof-matrix row (blocking)
  - [x] 1.1 Open `docs/evidence/AF-03-01.md` and locate the proof-matrix row for **AC2 — 60 FPS compliance is measurable**
  - [x] 1.2 Remove or replace **Failure Coverage** text that describes `unsupported capture mode rejected` for AC2; that failure story belongs to AC1 / OpenCV selection, not `test_capture_stability.py`
  - [x] 1.3 Align **Evidence** and **Entry Point** with what `tests/integration/test_capture_stability.py` actually proves (e.g. sustained-drop flagging, tracker/snapshot behavior at 60 FPS target, instability/fallback), **or** split AC2 into separate rows if the pack must cover both metrics primitives and OpenCV session metrics
  - [x] 1.4 If the intent of AC2 includes measurability on the OpenCV capture path, add a matrix row or extend **Evidence** to cite `test_opencv_capture_start_stop_yields_measurable_metrics` in `test_capture_opencv.py`

- [x] 2.0 Tighten performance **Threshold** prose for sustained drop (recommended)
  - [x] 2.1 In `docs/evidence/AF-03-01.md`, update threshold language for artifacts tied to `is_sustained_drop` so it matches `CaptureMetrics` in `src/aetherflow/core/capture_metrics.py`: sustained drop when `measured_fps < 0.9 * target_fps` with `duration_s >= 3.0`, and/or the drop-rate branch (`drop_rate > 0.02` with `drop_window_s >= 5.0`), not a vague “measured_fps < target_fps” rule

- [x] 3.0 Optional: clarify AC1 scope (UI vs plugin/API)
  - [ ] 3.1 If AC1 is meant to include UI “selectable” behavior, add a proof-matrix row or extend **Evidence** to mention `tests/ui/test_capture_mode_matrix.py` (e.g. panel hides unsupported combinations)
  - [x] 3.2 If AC1 is plugin/API-only, add a short explicit note in the evidence pack so reviewers do not expect UI proof in that row

- [x] 4.0 Regenerate verification artifacts
  - [x] 4.1 Run `uv run python -m tools.verify_requirements` and confirm it completes successfully
  - [x] 4.2 Spot-check `logs/verification/AF-03-01.json` and `docs/requirements-report.md` for AF-03-01 consistency with the updated evidence pack

- [ ] 5.0 Reviewer sign-off (after matrix is accurate)
  - [ ] 5.1 Have a human reviewer confirm the proof matrix matches cited tests and performance prose matches `capture_metrics.py`
  - [ ] 5.2 Complete reviewer fields and approval in `docs/evidence/AF-03-01.md` per project convention, then re-run `uv run python -m tools.verify_requirements` if sign-off affects verifier output
