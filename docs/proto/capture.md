# Capture Control Plane

`proto/capture.proto` defines the control-plane messages for capture sessions,
worker heartbeats, and log forwarding. Generated Python modules should be placed
under `src/aetherflow/core/ipc/`.
